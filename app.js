// Escribe tu código aquí.

const myArray = ['Computadora', 'Pozole', 'tablet', 'perro', 'gato', 'celular'];

function palabraGrande (str, arr){

    return arr.filter((word) => word.lengh > str.lengh); 
}

console.log('tablet', palabraGrande);

function impArray (arr ){

    const ul = documet.createElement ('ul');
    arr.((word) => {

        const li = document.createElement('')
    })
}